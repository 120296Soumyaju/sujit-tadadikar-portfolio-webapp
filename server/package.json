{
  "name": "server",
  "version": "1.0.0",
  "type": "module",
  "description": "This is a simple Express server that handles sending emails from the portfolio contact form using Nodemailer.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "nodemailer": "^7.0.3"
  }
}
